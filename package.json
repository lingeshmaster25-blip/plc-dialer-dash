// top level — add:
"main": "electron/main.js",

// scripts — add:
"electron:dev": "concurrently \"vite --config vite.config.electron.ts\" \"wait-on http://localhost:5173 && electron .\"",
"electron:build:spa": "vite build --config vite.config.electron.ts",
"electron:build:win": "npm run electron:build:spa && electron-builder --win --config electron-builder.json",

// devDependencies — add:
"electron": "^31.0.0",
"electron-builder": "^24.13.3",
"concurrently": "^8.2.2",
"wait-on": "^7.2.0"
